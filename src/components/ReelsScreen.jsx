import React, { useEffect, useRef, useState } from 'react';
import { CheckCircle, Heart, MessageCircle, Share2, Plus, X, Trash2, Volume2, Pause, Play } from 'lucide-react';
import { supabase } from '../services/supabase';
import { uploadToCloudinary } from '../services/cloudinary';
import { useAuth } from '../context/AuthContext';

const timeAgo = (value) => {
  const minutes = Math.floor((Date.now() - new Date(value).getTime()) / 60000);
  if (minutes < 1) return 'now';
  if (minutes < 60) return `${minutes}m ago`;
  if (minutes < 1440) return `${Math.floor(minutes / 60)}h ago`;
  return `${Math.floor(minutes / 1440)}d ago`;
};

export default function Reels() {
  const { user } = useAuth();
  const [reels, setReels] = useState([]);
  const [caption, setCaption] = useState('');
  const [videoFile, setVideoFile] = useState(null);
  const [commentFor, setCommentFor] = useState(null);
  const [comment, setComment] = useState('');
  const [showComposer, setShowComposer] = useState(false);
  const [error, setError] = useState('');
  const [uploading, setUploading] = useState(false);
  const [playing, setPlaying] = useState({});
  const [progress, setProgress] = useState({});
  const [followed, setFollowed] = useState({});
  const [likedAt, setLikedAt] = useState({});
  const videoRefs = useRef({});

  const loadReels = async () => {
    const { data, error: queryError } = await supabase.from('reels').select('id, user_id, caption, video_url, created_at, reel_comments(id, user_id, content, created_at), reel_likes(user_id)').order('created_at', { ascending: false });
    if (queryError) { setError(queryError.message); return; }
    const ids = [...new Set((data || []).flatMap((reel) => [reel.user_id, ...(reel.reel_comments || []).map((item) => item.user_id), ...(reel.reel_likes || []).map((item) => item.user_id)]))];
    const { data: profileRows } = await supabase.from('profiles').select('id, display_name, avatar_url').in('id', ids);
    const profiles = new Map((profileRows || []).map((profile) => [profile.id, profile]));
    setReels((data || []).map((reel) => ({ ...reel, author: profiles.get(reel.user_id)?.display_name || 'MyChat User', avatar: profiles.get(reel.user_id)?.avatar_url || 'https://ui-avatars.com/api/?name=MyChat&background=4f46e5&color=fff', liked: (reel.reel_likes || []).some((like) => like.user_id === user.id), likeNames: (reel.reel_likes || []).map((like) => profiles.get(like.user_id)?.display_name || 'MyChat User'), comments: (reel.reel_comments || []).map((item) => ({ ...item, author: profiles.get(item.user_id)?.display_name || 'MyChat User' })) })));
  };

  useEffect(() => { loadReels(); const channel = supabase.channel('reels-live').on('postgres_changes', { event: '*', schema: 'public', table: 'reels' }, loadReels).on('postgres_changes', { event: '*', schema: 'public', table: 'reel_comments' }, loadReels).on('postgres_changes', { event: '*', schema: 'public', table: 'reel_likes' }, loadReels).subscribe(); return () => { supabase.removeChannel(channel); }; }, [user.id]);

  const postReel = async (event) => { event.preventDefault(); if (!caption.trim() || !videoFile) return; setUploading(true); try { const upload = await uploadToCloudinary(videoFile, 'video'); const { error: insertError } = await supabase.from('reels').insert({ user_id: user.id, caption: caption.trim(), video_url: upload.secure_url }); if (insertError) throw insertError; setCaption(''); setVideoFile(null); setShowComposer(false); } catch (uploadError) { setError(uploadError.message); } finally { setUploading(false); } };
  const toggleLike = async (reel) => { const result = reel.liked ? await supabase.from('reel_likes').delete().match({ reel_id: reel.id, user_id: user.id }) : await supabase.from('reel_likes').insert({ reel_id: reel.id, user_id: user.id }); if (result.error) setError(result.error.message); };
  const doubleTapLike = (reel) => { const now = Date.now(); if (now - (likedAt[reel.id] || 0) < 350 && !reel.liked) toggleLike(reel); setLikedAt((current) => ({ ...current, [reel.id]: now })); };
  const addComment = async (event) => { event.preventDefault(); if (!comment.trim()) return; const { error: insertError } = await supabase.from('reel_comments').insert({ reel_id: commentFor, user_id: user.id, content: comment.trim() }); if (insertError) setError(insertError.message); else { setComment(''); setCommentFor(null); } };
  const deleteReel = async (reelId) => { if (!window.confirm('Delete this reel? Select OK for Yes or Cancel for No.')) return; const { error: deleteError } = await supabase.from('reels').delete().eq('id', reelId).eq('user_id', user.id); if (deleteError) setError(deleteError.message); else setReels((current) => current.filter((reel) => reel.id !== reelId)); };
  const toggleVideo = (id) => { const video = videoRefs.current[id]; if (!video) return; if (video.paused) { video.play(); setPlaying((current) => ({ ...current, [id]: true })); } else { video.pause(); setPlaying((current) => ({ ...current, [id]: false })); } };
  const shareReel = async (reel) => { const url = `${window.location.origin}/?reel=${reel.id}`; if (navigator.share) await navigator.share({ title: reel.author, text: reel.caption, url }); else { await navigator.clipboard?.writeText(url); setError('Reel link copied'); } };

  return <div className="relative h-[100dvh] w-full overflow-y-scroll snap-y snap-mandatory bg-black text-white">{error && <p className="fixed top-3 left-3 right-3 z-50 mx-auto max-w-md rounded-lg bg-black/80 p-2 text-xs text-red-300">{error}</p>}<button onClick={() => setShowComposer(true)} aria-label="Post a reel" className="fixed top-4 right-4 z-40 rounded-full bg-indigo-600 p-3 text-white shadow-xl"><Plus className="h-5 w-5" /></button>{reels.map((reel) => <article key={reel.id} className="relative h-[100dvh] w-full snap-start bg-black"><video ref={(element) => { videoRefs.current[reel.id] = element; }} src={reel.video_url} className="h-full w-full object-cover" loop autoPlay muted playsInline onTimeUpdate={(event) => setProgress((current) => ({ ...current, [reel.id]: event.currentTarget.duration ? (event.currentTarget.currentTime / event.currentTarget.duration) * 100 : 0 }))} onPlay={() => setPlaying((current) => ({ ...current, [reel.id]: true }))} onPause={() => setPlaying((current) => ({ ...current, [reel.id]: false }))} onClick={() => { doubleTapLike(reel); toggleVideo(reel.id); }} /><div className="pointer-events-none absolute inset-0 bg-gradient-to-b from-black/25 via-transparent to-black/80" /><div className="absolute right-3 bottom-32 z-20 flex flex-col items-center gap-3"><button onClick={() => toggleLike(reel)} className={`pointer-events-auto rounded-full bg-black/35 p-3 ${reel.liked ? 'text-red-500' : 'text-white'}`}><Heart className="h-7 w-7" fill={reel.liked ? 'currentColor' : 'none'} /></button><span className="text-xs font-semibold">{reel.reel_likes?.length || 0}</span><button onClick={() => setCommentFor(reel.id)} className="rounded-full bg-black/35 p-3"><MessageCircle className="h-7 w-7" /></button><span className="text-xs font-semibold">{reel.comments.length}</span><button onClick={() => shareReel(reel)} className="rounded-full bg-black/35 p-3"><Share2 className="h-7 w-7" /></button><button onClick={() => setError('Sound: MyChat original audio')} className="rounded-full bg-black/35 p-3"><Volume2 className="h-6 w-6" /></button></div><div className="absolute bottom-6 left-4 right-20 z-20 space-y-2"><div className="flex items-center gap-3"><img src={reel.avatar} alt={reel.author} className="h-10 w-10 rounded-full border border-white/50" /><strong>{reel.author}</strong><CheckCircle className="h-4 w-4 fill-sky-400 text-white" /><button onClick={() => setFollowed((current) => ({ ...current, [reel.user_id]: !current[reel.user_id] }))} className="rounded-lg border border-white/60 px-2 py-1 text-xs">{followed[reel.user_id] ? 'Following' : 'Follow'}</button>{reel.user_id === user.id && <button onClick={() => deleteReel(reel.id)} aria-label="Delete reel" className="ml-auto"><Trash2 className="h-5 w-5 text-red-300" /></button>}</div><p className="text-sm">{reel.caption}</p><p className="flex items-center gap-1 text-xs text-slate-300"><Play className="h-3 w-3" /> MyChat original audio • {timeAgo(reel.created_at)}</p>{reel.comments.slice(-3).map((item) => <p key={item.id} className="text-xs"><b>{item.author}</b>: {item.content}</p>)}</div><div className="absolute bottom-0 left-0 right-0 z-30 h-1 bg-white/20"><div className="h-full bg-white" style={{ width: `${progress[reel.id] || 0}%` }} /></div>{!playing[reel.id] && <button onClick={() => toggleVideo(reel.id)} aria-label="Play reel" className="absolute left-1/2 top-1/2 z-20 -translate-x-1/2 -translate-y-1/2 rounded-full bg-black/50 p-4"><Play className="h-8 w-8" /></button>}</article>)}{!reels.length && <div className="flex h-full items-center justify-center text-sm text-slate-400">No reels yet. Upload the first one.</div>}{showComposer && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 p-4"><form onSubmit={postReel} className="w-full max-w-sm space-y-3 rounded-2xl bg-slate-900 p-5"><div className="flex justify-between"><h2 className="font-bold">Post a Reel</h2><button type="button" onClick={() => setShowComposer(false)}><X /></button></div><textarea value={caption} onChange={(event) => setCaption(event.target.value)} required placeholder="Write a caption" className="w-full rounded-xl bg-slate-950 p-3 text-sm" /><input type="file" accept="video/*" required onChange={(event) => setVideoFile(event.target.files?.[0] || null)} /><button disabled={uploading} className="w-full rounded-xl bg-indigo-600 py-3 font-semibold">{uploading ? 'Uploading...' : 'Upload Reel'}</button></form></div>}{commentFor && <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/70 p-4"><form onSubmit={addComment} className="flex w-full max-w-md gap-2 rounded-2xl bg-slate-900 p-4"><input autoFocus value={comment} onChange={(event) => setComment(event.target.value)} placeholder="Write a comment" className="flex-1 rounded-xl bg-slate-950 px-3 text-sm" /><button className="rounded-xl bg-indigo-600 px-4"><Send className="h-4 w-4" /></button><button type="button" onClick={() => setCommentFor(null)}><X /></button></form></div>}</div>;
}
